machine:
  services:
    - docker

dependencies:
  pre:
    - echo $GOPATH
    - go build init
    - docker build -t go-scratch -f Dockerfile --no-cache .
test:
  override:
    - docker run go-scratch
